export interface User {
  id: string;
  name: string;
  email: string;
  avatarUrl?: string;
  full_name: string;
  avatar_url?: string;
  phone?: string;
  address?: string;
  created_at: string;
  
}
